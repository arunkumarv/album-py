# source, destination and sqlite ready
# traverse through every file in the source directory
    # extract content from each file
    # move the file from source to destination with a rename
    # store the (destination_path, content) in sqlite
